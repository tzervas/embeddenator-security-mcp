# REUSE compliance — security-mcp

Full REUSE compliance is met! This project maintains REUSE compliance via package aggregate annotations in `REUSE.toml` rather than file-level SPDX headers.

Copyright: 2026 Tyler Zervas — SPDX-License-Identifier: MIT

All project files, including hidden files and directories (such as `.github/**`, `.cz.toml`, `.gitleaks.toml`, `.pre-commit-config.yaml`, and `.mcp.json.example`), are covered by the aggregate annotations in `REUSE.toml`. The REUSE compliance debt is fully resolved.
