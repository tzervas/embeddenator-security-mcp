# REUSE debt — security-mcp (P24f bootstrap)

Generated by P24f REUSE bootstrap. Full REUSE compliance is deferred;
this bootstrap adds `REUSE.toml` + `LICENSES/MIT.txt` package annotations.

Copyright: 2026 Tyler Zervas — SPDX-License-Identifier: MIT

Package-level aggregate annotations in `REUSE.toml` have been updated to cover `.github/**`, `.cz.toml`, `.gitleaks.toml`, `.mcp.json.example`, `.pre-commit-config.yaml`, `trivy-secret.yaml`, `LICENSES/**`, and `LICENSE`.
